import { Class, IClass, IClassModel } from '../../models/class.model';
import { Callback } from '../../interfaces/callback';

export const createClass = (class: IClass, callback: Callback): void => {
  
}